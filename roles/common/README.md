Данная роль содержит общие переменные и общие handlers.

Во всех ролях, где используются общие переменные или handlers необходимо создать `meta/main.yml` со следующим содержанием:
```
dependencies:
  - daowolf2000.linux.common
```
