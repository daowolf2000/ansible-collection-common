127.0.0.1   localhost
127.0.1.1   {{ inventory_hostname }} {{ inventory_hostname }}.{{ network_domain_full | lower() }}

{% for host in groups['all']|sort() %}
{{ hostvars[host].ansible_host }}   {{host}} {{ host }}.{{ network_domain_full | lower() }} 
{% endfor %}

# The following lines are desirable for IPv6 capable hosts
::1     localhost ip6-localhost ip6-loopback
ff02::1 ip6-allnodes
ff02::2 ip6-allrouters
